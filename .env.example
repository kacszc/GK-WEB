# URL backendu (Spring Boot). Używane przez src/lib/api-client.ts gdy serwisy przejdą z mocków na HTTP.
# Routes live under /api/... so do NOT include the /api suffix here.
NEXT_PUBLIC_API_URL=http://localhost:8080
